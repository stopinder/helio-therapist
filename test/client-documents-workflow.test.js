import test from 'node:test';
import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
const read=path=>readFile(new URL(path,import.meta.url),'utf8');
test('client document schema is additive, client-scoped and draft-capable',async()=>{const migration=await read('../supabase/migrations/20260809170000_harden_client_documents.sql');assert.match(migration,/add column if not exists client_id uuid references public\.clients\(id\)/);assert.match(migration,/content jsonb/);assert.match(migration,/source_manifest jsonb/);assert.match(migration,/version integer/);assert.match(migration,/alter column storage_path drop not null/);assert.match(migration,/documents_user_client_created_idx/);assert.match(migration,/c\.user_id = auth\.uid\(\)/);});
test('document service only offers completed clinical sessions as sources',async()=>{const service=await read('../src/lib/clientDocuments.js');assert.match(service,/from\('sessions'\)/);assert.match(service,/\.eq\('client_id',\s*clientId\)/);assert.match(service,/\.eq\('status',\s*'completed'\)/);assert.match(service,/notes,notes_status,version/);assert.doesNotMatch(service,/private_reflections/);assert.doesNotMatch(service,/included_in_supervision/);assert.match(service,/DOCUMENT_CONFLICT/);assert.match(service,/\.eq\('version',\s*document\.version\)/);});
test('Client Workspace owns the single composer and exposes safe document actions',async()=>{const header=await read('../src/components/workspace/ClientWorkspaceHeader.vue');const composer=await read('../src/components/workspace/ClientDocumentComposer.vue');const panel=await read('../src/components/workspace/ClientDocumentsPanel.vue');const workspace=await read('../src/views/ClientWorkspace.vue');assert.match(header,/data-testid="create-client-document"/);assert.doesNotMatch(header,/ClientDocumentComposer/);assert.match(header,/create-document/);assert.match(workspace,/@create-document="newDocument"/);assert.match(workspace,/ClientDocumentComposer/);assert.match(composer,/data-testid="client-document-composer"/);assert.match(composer,/Only completed clinical session notes/);assert.match(composer,/Private reflections and supervision material are not available here/);assert.match(composer,/Save Draft/);assert.match(composer,/Preview PDF/);assert.match(composer,/Finalise PDF/);assert.match(composer,/finaliseClientDocument/);assert.match(composer,/You have unsaved changes/);assert.match(composer,/document-finalised-success/);assert.match(composer,/saved to Client Documents/);assert.match(composer,/Download PDF/);assert.match(composer,/View Client Documents/);assert.doesNotMatch(composer,/emit\('close'\).*finalise/s);assert.match(panel,/data-testid="client-documents-panel"/);assert.match(panel,/Edit Draft/);assert.match(workspace,/activeTab === 'Documents'/);assert.match(workspace,/listClientDocuments/);assert.match(workspace,/downloadClientDocument/);});
test('clinical source selector stays compact and prevents duplicate note insertion',async()=>{const composer=await read('../src/components/workspace/ClientDocumentComposer.vue');assert.match(composer,/data-testid="clinical-source-panel"/);assert.match(composer,/data-testid="clinical-source-list"/);assert.match(composer,/overflow-y-auto/);assert.match(composer,/Search session notes/);assert.match(composer,/Select all shown/);assert.match(composer,/Preview/);assert.match(composer,/expandedSourceId/);assert.match(composer,/Sources: \{\{ selectedSourceIds\.length \}\}/);assert.match(composer,/Add selected notes to draft/);assert.match(composer,/addedSourceIds/);assert.match(composer,/addableSelectedSources/);assert.match(composer,/Selected notes already added/);assert.match(composer,/load-more-clinical-sources/);assert.doesNotMatch(composer,/line-clamp-4/);});
test('preview and finalisation share a formatted embedded-font PDF renderer',async()=>{const api=await read('../api/documents.js');const renderer=await read('../api/_lib/documentPdf.js');const service=await read('../src/lib/clientDocuments.js');const composer=await read('../src/components/workspace/ClientDocumentComposer.vue');assert.match(api,/buildDocumentPdf/);assert.match(api,/req\.query\.preview/);assert.match(api,/await buildDocumentPdf\(draft\)/);assert.match(api,/Content-Disposition','inline/);assert.match(api,/eq\('user_id',user\.id\)/);assert.match(api,/expectedVersion/);assert.match(api,/status:'completed'/);assert.match(api,/contentType:'application\/pdf'/);assert.match(api,/createSignedUrl/);assert.match(renderer,/PDFDocument/);assert.match(renderer,/@fontsource\/noto-sans/);assert.match(renderer,/registerFont\('NotoSans'/);assert.match(renderer,/registerFont\('NotoSansBold'/);assert.match(renderer,/Confidential clinical document/);assert.match(renderer,/pageAdded/);assert.doesNotMatch(renderer,/replace\(\/\[\^\\x20-\\x7E/);assert.match(service,/\/api\/documents\?preview=/);assert.match(service,/\/api\/documents\?finalize=/);assert.match(service,/\/api\/documents\?download=/);assert.doesNotMatch(service,/window\.open/);assert.match(composer,/data-testid="document-pdf-preview"/);assert.match(composer,/same renderer used for the final PDF/);assert.match(composer,/iframe/);});
