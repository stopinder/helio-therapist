<template><component :is="to ? RouterLink : 'button'" :to="to || undefined" :type="to ? undefined : type" :disabled="to ? undefined : disabled" :aria-disabled="disabled || undefined" :class="['inline-flex min-h-touch items-center justify-center gap-2', variant === 'primary' ? 'button-primary' : 'button-secondary', disabled && 'pointer-events-none']"><slot /></component></template>
<script setup>import { RouterLink } from 'vue-router'; defineProps({ variant: { type: String, default: 'secondary', validator: v => ['primary','secondary'].includes(v) }, to: { type: [String,Object], default: null }, type: { type: String, default: 'button' }, disabled: { type: Boolean, default: false } })</script>
