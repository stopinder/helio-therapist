Warning: truncated output (original token count: 838)
Total output lines: 100

import OpenAI from "openai";

const openai = new OpenAI({
    apiKey: process.env.OPENAI_API_KEY,
});

export default async function handler(req, res) {
    if (req.method !== "POST") {
        return…738 tokens truncated…. Preserve uncertainty. Do not diagnose the therapist or client, make clinical decisions, or present inferences as facts. This is an editable draft and must not alter the original reflection.`;
    }
